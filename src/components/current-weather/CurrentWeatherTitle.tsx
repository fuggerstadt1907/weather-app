type Props = {};

const CurrentWeatherTitle: React.FC<Props> = ({}) => {
  return (
    <h2 className="text-xl font-semibold text-black dark:text-gray-300 mb-4">
      Aktuelles Wetter
    </h2>
  );
};

export default CurrentWeatherTitle;
